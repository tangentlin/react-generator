import React from 'react';
import { <%= classify(name) %> } from './<%= classify(name) %>';

export default {
  title: '<%= classify(name) %>',
  component: <%= classify(name) %>,
};

export const defaultStory = () => <<%= classify(name) %> />;
defaultStory.storyName = 'Default';

